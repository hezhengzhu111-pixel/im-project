name: im_mobile_flutter
description: IM mobile client aligned with existing frontend capabilities
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.9.0
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  web_socket_channel: ^3.0.3
  uuid: ^4.5.1
  image_picker: ^1.2.0
  file_picker: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
