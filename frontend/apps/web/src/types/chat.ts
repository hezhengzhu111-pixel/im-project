/**
 * 会话相关类型定义
 *
 * 已迁移至 @im/shared-types，此处仅为 re-export 以保持向后兼容。
 */

export type {
  ChatSessionType,
  ChatSession,
  RawConversationDTO,
  OnlineStatus,
  WebSocketMessage,
  GroupReadUser,
  Conversation,
} from '@im/shared-types';
