import { http } from "@/utils/request";

export const imService = {
  // IM specific operations if any, currently mostly covered by message and user services
  // Placeholder for direct IM controller calls if needed separately
};
