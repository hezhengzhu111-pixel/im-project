{
  "name": "im-project-tests",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "deploy:middleware": "python scripts/deploy_middleware.py --clean",
    "deploy:services": "python scripts/deploy_services.py",
    "deploy:services:initdb": "python scripts/deploy_services.py --init-db"
  }
}
