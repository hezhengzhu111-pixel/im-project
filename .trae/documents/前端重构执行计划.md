# 前端代码重构 - 可执行计划

## 项目信息
- **项目路径**: `c:\Users\10954\.openclaw\workspace\new-im-project\frontend`
- **重构周期**: 4周
- **任务总数**: 40+ 项

---

## 阶段一：类型系统重构（第1周）

### 任务 1.1: 创建 src/types/user.ts
**优先级**: HIGH | **预计时间**: 1小时

**操作步骤**:
1. 创建文件 `src/types/user.ts`
2. 从 `src/types/index.ts` 提取 User 相关类型
3. 合并重复的 `User` 和 `UserInfo` 接口

**目标代码结构**:
```typescript
// src/types/user.ts
export interface User {
  id: string;
  username: string;
  nickname: string;
  avatar?: string;
  email?: string;
  phone?: string;
  gender?: string;
  birthday?: string;
  signature?: string;
  location?: string;
  lastSeen?: string;
  status: UserStatus;
  lastLoginTime?: string;
  createTime?: string;
}

export type UserStatus = 'ONLINE' | 'OFFLINE' | 'BUSY' | 'AWAY';

export interface LoginRequest {
  username: string;
  password: string;
}

export interface RegisterRequest {
  username: string;
  password: string;
  nickname: string;
  email?: string;
  phone?: string;
}
// ... 更多类型
```

---

### 任务 1.2: 创建 src/types/message.ts
**优先级**: HIGH | **预计时间**: 1.5小时

**操作步骤**:
1. 创建文件 `src/types/message.ts`
2. 定义完整的 Message 类型，移除 any
3. 添加类型守卫

**目标代码结构**:
```typescript
// src/types/message.ts
export type MessageType = 'TEXT' | 'IMAGE' | 'FILE' | 'VIDEO' | 'VOICE' | 'SYSTEM';
export type MessageStatus = 'SENDING' | 'SENT' | 'DELIVERED' | 'READ' | 'FAILED';

export interface Message {
  id: string | number;
  senderId: string | number;
  senderName?: string;
  senderAvatar?: string;
  receiverId?: string | number;
  groupId?: string | number;
  isGroupChat: boolean;
  messageType: MessageType;
  content: string;
  mediaUrl?: string;
  mediaSize?: number;
  mediaName?: string;
  thumbnailUrl?: string;
  duration?: number;
  sendTime: string;
  status?: MessageStatus;
  extra?: Record<string, unknown>;
}

export interface SendMessageRequest {
  receiverId?: string;
  groupId?: string;
  isGroupChat: boolean;
  type: MessageType;
  content: string;
  extra?: Record<string, unknown>;
}
```

---

### 任务 1.3: 创建 src/types/chat.ts
**优先级**: HIGH | **预计时间**: 1小时

**操作步骤**:
1. 创建文件 `src/types/chat.ts`
2. 提取 ChatSession、ChatItem 等类型

---

### 任务 1.4: 创建 src/types/group.ts
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 创建文件 `src/types/group.ts`
2. 从 `src/types/index.ts` 提取 Group 相关类型

---

### 任务 1.5: 创建 src/types/api.ts
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 创建文件 `src/types/api.ts`
2. 定义统一的 ApiResponse 类型

```typescript
// src/types/api.ts
export interface ApiResponse<T = unknown> {
  code: number;
  message: string;
  data: T;
  timestamp: number;
  success?: boolean;
}

export interface PageRequest {
  page: number;
  size: number;
  sort?: string;
  order?: 'asc' | 'desc';
}

export interface PageResponse<T> {
  content: T[];
  totalElements: number;
  totalPages: number;
  page: number;
  size: number;
}
```

---

### 任务 1.6: 创建 src/types/common.ts
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 创建文件 `src/types/common.ts`
2. 提取分页、文件上传等公共类型

---

### 任务 1.7: 创建 src/types/utils.ts
**优先级**: MEDIUM | **预计时间**: 1小时

**操作步骤**:
1. 创建文件 `src/types/utils.ts`
2. 添加类型守卫函数

```typescript
// src/types/utils.ts
export function isMessage(obj: unknown): obj is Message {
  if (!obj || typeof obj !== 'object') return false;
  const msg = obj as Record<string, unknown>;
  return (
    'id' in msg &&
    'senderId' in msg &&
    'messageType' in msg &&
    'content' in msg &&
    'sendTime' in msg
  );
}

export function isUser(obj: unknown): obj is User {
  if (!obj || typeof obj !== 'object') return false;
  const user = obj as Record<string, unknown>;
  return 'id' in user && 'username' in user && 'nickname' in user;
}

export type PartialBy<T, K extends keyof T> = Omit<T, K> & Partial<Pick<T, K>>;
export type RequiredBy<T, K extends keyof T> = Omit<T, K> & Required<Pick<T, K>>;
```

---

### 任务 1.8: 重构 src/types/index.ts
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 编辑 `src/types/index.ts`
2. 改为重新导出所有类型

```typescript
// src/types/index.ts
export * from './user';
export * from './message';
export * from './chat';
export * from './group';
export * from './api';
export * from './common';
export * from './utils';
```

---

## 阶段二：Store 拆分重构（第2周）

### 任务 2.1: 从 useChatStore 拆分出 useMessageStore
**优先级**: HIGH | **预计时间**: 3小时

**操作步骤**:
1. 创建新文件 `src/stores/message.ts`
2. 提取消息相关状态和操作:
   - `messages` Map
   - `loadMessages()`
   - `sendSingleMessage()`
   - `deleteMessage()`
   - `searchMessages()`
   - `markAsRead()`
3. 保留对其他 store 的引用

**新文件结构**:
```typescript
// src/stores/message.ts
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import { messageService } from '@/services';
import { messageRepo } from '@/utils/messageRepo';
import { normalizeMessageBase } from '@/utils/messageNormalize';
import { useUserStore } from './user';
import { ElMessage } from 'element-plus';

export const useMessageStore = defineStore('message', () => {
  const messages = ref<Map<string, Message[]>>(new Map());
  const loading = ref(false);

  // ... 消息操作方法

  return { messages, loading, loadMessages, sendMessage, deleteMessage, ... };
});
```

---

### 任务 2.2: 从 useChatStore 拆分出 useSessionStore
**优先级**: HIGH | **预计时间**: 2小时

**操作步骤**:
1. 创建新文件 `src/stores/session.ts`
2. 提取会话相关状态和操作:
   - `sessions` 列表
   - `currentSession`
   - `sortedSessions` 计算属性
   - `createOrGetSession()`
   - `setCurrentSession()`
   - `loadSessions()`
   - 会话排序、置顶、静音逻辑

---

### 任务 2.3: 重构 useChatStore
**优先级**: HIGH | **预计时间**: 2小时

**操作步骤**:
1. 编辑 `src/stores/chat.ts`
2. 移除已拆分到 message.ts 和 session.ts 的代码
3. 保留跨 store 协调逻辑（init, clear）
4. 委托操作到子 store

---

### 任务 2.4: 精简 useUserStore
**优先级**: MEDIUM | **预计时间**: 2小时

**操作步骤**:
1. 编辑 `src/stores/user.ts`
2. 移除设置相关方法（getUserSettings, updateSettings 等）
3. 移除验证码相关方法（sendEmailCode, sendPhoneCode, bindEmail, bindPhone）
4. 这些方法将移至独立 store

---

### 任务 2.5: 创建 useSettingsStore
**优先级**: MEDIUM | **预计时间**: 1.5小时

**操作步骤**:
1. 创建新文件 `src/stores/settings.ts`
2. 将用户设置相关逻辑从 user.ts 移入

```typescript
// src/stores/settings.ts
export const useSettingsStore = defineStore('settings', () => {
  const settings = ref<UserSettings>({...});

  const updatePrivacySettings = async (data: Record<string, boolean>) => {...};
  const updateMessageSettings = async (data: Record<string, boolean>) => {...};
  const updateGeneralSettings = async (data: Record<string, unknown>) => {...};

  return { settings, updatePrivacySettings, ... };
});
```

---

### 任务 2.6: 重构 useWebSocketStore
**优先级**: MEDIUM | **预计时间**: 2小时

**操作步骤**:
1. 编辑 `src/stores/websocket.ts`
2. 简化消息处理逻辑
3. 添加消息去重机制
4. 优化 recentMessageIds 清理

---

## 阶段三：Composables 提取（第3周）

### 任务 3.1: 创建 useMessageActions.ts
**优先级**: HIGH | **预计时间**: 2小时

**操作步骤**:
1. 创建 `src/composables/useMessageActions.ts`
2. 从 `Chat.vue` 和 `useChatLogic.ts` 提取消息操作逻辑

```typescript
// src/composables/useMessageActions.ts
export function useMessageActions() {
  const chatStore = useChatStore();
  const { sendMessage, deleteMessage, recallMessage } = useMessageStore();

  const handleSendMessage = async (content: string, type: MessageType = 'TEXT') => {
    // 发送消息逻辑
  };

  const handleRecall = async (messageId: string) => {
    // 撤回消息逻辑
  };

  return { handleSendMessage, handleRecall, ... };
}
```

---

### 任务 3.2: 创建 useSessionManager.ts
**优先级**: HIGH | **预计时间**: 1.5小时

**操作步骤**:
1. 创建 `src/composables/useSessionManager.ts`
2. 提取会话管理逻辑

---

### 任务 3.3: 创建 useAudioPlayer.ts
**优先级**: MEDIUM | **预计时间**: 1.5小时

**操作步骤**:
1. 创建 `src/composables/useAudioPlayer.ts`
2. 从 `MessageItem.vue` 提取音频播放逻辑
3. 确保正确的资源清理

```typescript
// src/composables/useAudioPlayer.ts
export function useAudioPlayer() {
  const audioPlayer = ref<HTMLAudioElement | null>(null);
  const isPlaying = ref(false);

  const play = (url: string) => {...};
  const pause = () => {...};
  const stop = () => {...};

  onUnmounted(() => {
    // 确保资源清理
    stop();
    audioPlayer.value = null;
  });

  return { isPlaying, play, pause, stop };
}
```

---

### 任务 3.4: 创建 useContextMenu.ts
**优先级**: MEDIUM | **预计时间**: 1小时

**操作步骤**:
1. 创建 `src/composables/useContextMenu.ts`
2. 从 `MessageItem.vue` 提取右键菜单逻辑
3. 确保正确的事件监听清理

---

### 任务 3.5: 创建 useScrollPagination.ts
**优先级**: MEDIUM | **预计时间**: 1.5小时

**操作步骤**:
1. 创建 `src/composables/useScrollPagination.ts`
2. 封装滚动加载逻辑

```typescript
// src/composables/useScrollPagination.ts
export function useScrollPagination(
  containerRef: Ref<HTMLElement | null>,
  loadMore: (page: number) => Promise<void>
) {
  const isLoading = ref(false);
  const currentPage = ref(0);

  const handleScroll = async () => {
    if (isLoading.value) return;
    // 检测是否滚动到顶部，加载更多
  };

  return { isLoading, handleScroll };
}
```

---

### 任务 3.6: 创建 useErrorHandler.ts
**优先级**: MEDIUM | **预计时间**: 1小时

**操作步骤**:
1. 创建 `src/composables/useErrorHandler.ts`
2. 统一错误处理

---

## 阶段四：组件重构（第3周）

### 任务 4.1: 拆分 Chat.vue - ChatContainer
**优先级**: HIGH | **预计时间**: 2小时

**操作步骤**:
1. 创建 `src/components/chat/ChatContainer.vue`
2. 从 `Chat.vue` 提取主容器逻辑
3. 保留模板中的结构，委托逻辑给子组件

---

### 任务 4.2: 拆分 Chat.vue - ChatMessageList
**优先级**: HIGH | **预计时间**: 2小时

**操作步骤**:
1. 创建 `src/components/chat/ChatMessageList.vue`
2. 提取消息列表渲染逻辑
3. 保持与 MessageItem 的接口

---

### 任务 4.3: 重构 MessageItem.vue
**优先级**: MEDIUM | **预计时间**: 1.5小时

**操作步骤**:
1. 编辑 `src/components/MessageItem.vue`
2. 移除音频播放逻辑 → 使用 useAudioPlayer
3. 移除右键菜单逻辑 → 使用 useContextMenu
4. 保持纯展示组件职责

---

### 任务 4.4: 创建 VirtualMessageList.vue
**优先级**: MEDIUM | **预计时间**: 3小时

**操作步骤**:
1. 创建 `src/components/chat/VirtualMessageList.vue`
2. 实现虚拟滚动
3. 保持与 ChatMessageList 相同接口

---

## 阶段五：工具函数规范化（第4周）

### 任务 5.1: 拆分 common.ts
**优先级**: MEDIUM | **预计时间**: 1.5小时

**操作步骤**:
1. 创建 `src/utils/format.ts` - 格式化相关函数
2. 创建 `src/utils/validation.ts` - 验证相关函数
3. 编辑 `src/utils/common.ts` 改为重新导出

---

### 任务 5.2: 创建常量统一管理
**优先级**: MEDIUM | **预计时间**: 1小时

**操作步骤**:
1. 创建 `src/constants/index.ts`
2. 创建 `src/constants/message.ts`
3. 创建 `src/constants/api.ts`
4. 从 config/index.ts 迁移常量

---

### 任务 5.3: 统一 request.ts 错误处理
**优先级**: MEDIUM | **预计时间**: 1小时

**操作步骤**:
1. 编辑 `src/utils/request.ts`
2. 使用 useErrorHandler 统一错误处理
3. 规范化错误日志

---

## 阶段六：安全与性能优化（第4周）

### 任务 6.1: 添加生产环境 console 控制
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 创建 `src/utils/logger.ts`
2. 生产环境禁用 console.log
3. 替换关键文件的 console 调用

```typescript
// src/utils/logger.ts
const isDev = import.meta.env.DEV;

export const logger = {
  log: (...args: unknown[]) => isDev && console.log(...args),
  warn: (...args: unknown[]) => isDev && console.warn(...args),
  error: (...args: unknown[]) => console.error(...args),
};
```

---

### 任务 6.2: 优化消息列表渲染性能
**优先级**: MEDIUM | **预计时间**: 2小时

**操作步骤**:
1. 在 ChatMessageList 使用 `v-memo` 优化
2. 添加消息分页加载
3. 实现骨架屏

---

### 任务 6.3: 完善 useAudioPlayer 资源清理
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 确保 audioPlayer.onended 和 audioPlayer.onerror 正确清理

---

### 任务 6.4: 完善 useContextMenu 事件清理
**优先级**: MEDIUM | **预计时间**: 0.5小时

**操作步骤**:
1. 确保 document click 和 contextmenu 事件正确清理

---

## 执行检查清单

### 阶段一完成检查
- [ ] `src/types/user.ts` 创建完成
- [ ] `src/types/message.ts` 创建完成
- [ ] `src/types/chat.ts` 创建完成
- [ ] `src/types/group.ts` 创建完成
- [ ] `src/types/api.ts` 创建完成
- [ ] `src/types/common.ts` 创建完成
- [ ] `src/types/utils.ts` 创建完成
- [ ] `src/types/index.ts` 重新导出完成
- [ ] 运行 `npm run typecheck` 无错误

### 阶段二完成检查
- [ ] `src/stores/message.ts` 创建完成
- [ ] `src/stores/session.ts` 创建完成
- [ ] `src/stores/chat.ts` 重构完成
- [ ] `src/stores/settings.ts` 创建完成
- [ ] `src/stores/user.ts` 精简完成
- [ ] `src/stores/websocket.ts` 重构完成
- [ ] 所有 store 正常工作

### 阶段三完成检查
- [ ] `src/composables/useMessageActions.ts` 创建完成
- [ ] `src/composables/useSessionManager.ts` 创建完成
- [ ] `src/composables/useAudioPlayer.ts` 创建完成
- [ ] `src/composables/useContextMenu.ts` 创建完成
- [ ] `src/composables/useScrollPagination.ts` 创建完成
- [ ] `src/composables/useErrorHandler.ts` 创建完成

### 阶段四完成检查
- [ ] `src/components/chat/ChatContainer.vue` 创建完成
- [ ] `src/components/chat/ChatMessageList.vue` 创建完成
- [ ] `src/components/MessageItem.vue` 重构完成
- [ ] `src/components/chat/VirtualMessageList.vue` 创建完成
- [ ] 组件功能正常

### 阶段五完成检查
- [ ] `src/utils/format.ts` 创建完成
- [ ] `src/utils/validation.ts` 创建完成
- [ ] `src/constants/` 目录创建完成
- [ ] `src/utils/request.ts` 统一完成

### 阶段六完成检查
- [ ] `src/utils/logger.ts` 创建完成
- [ ] 生产环境无敏感日志
- [ ] 消息列表性能优化完成
- [ ] 资源泄漏问题修复

---

## 每日任务分配示例

### 第1周日程
| 日期 | 上午 | 下午 |
|------|------|------|
| 周一 | 任务 1.1-1.2 | 任务 1.3 |
| 周二 | 任务 1.4-1.5 | 任务 1.6-1.7 |
| 周三 | 任务 1.8 | 类型系统测试 |
| 周四 | 任务 2.1 | 任务 2.1 续 |
| 周五 | 任务 2.2 | 任务 2.2 续 |

### 第2周日程
| 日期 | 上午 | 下午 |
|------|------|------|
| 周一 | 任务 2.3 | 任务 2.4 |
| 周二 | 任务 2.5 | 任务 2.6 |
| 周三 | Store 集成测试 | 修复问题 |
| 周四 | 任务 3.1 | 任务 3.2 |
| 周五 | 任务 3.3 | 任务 3.4 |

### 第3周日程
| 日期 | 上午 | 下午 |
|------|------|------|
| 周一 | 任务 3.5-3.6 | Composables 测试 |
| 周二 | 任务 4.1 | 任务 4.2 |
| 周三 | 任务 4.2 续 | 任务 4.3 |
| 周四 | 任务 4.4 | 任务 4.4 续 |
| 周五 | 组件集成测试 | 修复问题 |

### 第4周日程
| 日期 | 上午 | 下午 |
|------|------|------|
| 周一 | 任务 5.1 | 任务 5.2 |
| 周二 | 任务 5.3 | 任务 6.1 |
| 周三 | 任务 6.2 | 任务 6.3-6.4 |
| 周四 | 性能测试 | 修复问题 |
| 周五 | 最终测试 | 文档更新 |

---

## 关键里程碑

1. **M1 (第1周末)**: 类型系统重构完成，所有类型定义拆分
2. **M2 (第2周末)**: Store 拆分完成，职责单一化
3. **M3 (第3周末)**: Composables 提取完成，组件拆分完成
4. **M4 (第4周末)**: 工具函数规范化完成，性能优化完成

---

## 回滚策略

如果重构过程中出现问题：
1. 每个任务完成后运行 `npm run typecheck` 和 `npm run test`
2. 使用 Git 分支管理，每次完成一个任务组后合并
3. 保持 API 层兼容性，避免破坏性更改
4. 预留 20% 时间处理意外问题
