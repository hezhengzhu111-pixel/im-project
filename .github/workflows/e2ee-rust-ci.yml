name: E2EE Rust CI

on:
  push:
    paths:
      - 'rust/crates/im-e2ee-core/**'
      - 'rust/crates/im-e2ee-ffi/**'
      - 'rust/crates/im-e2ee-wasm/**'
      - '.github/workflows/e2ee-rust-ci.yml'
  pull_request:
    paths:
      - 'rust/crates/im-e2ee-core/**'
      - 'rust/crates/im-e2ee-ffi/**'
      - 'rust/crates/im-e2ee-wasm/**'
      - '.github/workflows/e2ee-rust-ci.yml'
  workflow_dispatch:

jobs:
  e2ee-rust-core:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rust

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        run: |
          rustup update stable
          rustup default stable
          rustup component add clippy rustfmt

      - name: Format check (e2ee crates only)
        run: |
          cargo fmt --check -p im-e2ee-core
          cargo fmt --check -p im-e2ee-ffi
          cargo fmt --check -p im-e2ee-wasm

      - name: Clippy im-e2ee-core
        run: cargo clippy -p im-e2ee-core -- -D warnings

      - name: Test im-e2ee-core
        run: cargo test -p im-e2ee-core

      - name: Test im-e2ee-ffi
        run: cargo test -p im-e2ee-ffi

      - name: Test im-e2ee-wasm
        run: cargo test -p im-e2ee-wasm

      - name: Check e2ee-wasm with wasm32 target
        run: |
          rustup target add wasm32-unknown-unknown
          cargo check -p im-e2ee-wasm --target wasm32-unknown-unknown

      - name: Scan im-e2ee-core/src for forbidden patterns
        run: |
          PATTERNS='\.unwrap\(|\.expect\(|\bunsafe\b|panic!|unreachable!|todo!|unimplemented!'
          MATCHES=$(grep -nE "$PATTERNS" crates/im-e2ee-core/src/*.rs | grep -v 'forbid(unsafe_code)') || true
          if [ -n "$MATCHES" ]; then
            echo "$MATCHES"
            echo "::error::Forbidden patterns found in crates/im-e2ee-core/src/"
            exit 1
          fi
          echo "PASS: crates/im-e2ee-core/src is clean"

      - name: Scan im-e2ee-ffi/im-e2ee-wasm src for forbidden patterns (report only)
        run: |
          PATTERNS='\.unwrap\(|\.expect\(|\bunsafe\b|panic!|unreachable!|todo!|unimplemented!'
          echo "=== crates/im-e2ee-ffi/src ==="
          grep -nE "$PATTERNS" crates/im-e2ee-ffi/src/*.rs || echo "  clean"
          echo "=== crates/im-e2ee-wasm/src ==="
          grep -nE "$PATTERNS" crates/im-e2ee-wasm/src/*.rs || echo "  clean"
