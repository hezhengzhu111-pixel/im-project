# 模块联调报告：登录与用户

## 基线信息

- 网关地址: ${gateway_host}:${gateway_port}
- 验证时间: ${date}
- 版本号: ${version}
- 验证人: ${owner}

## 覆盖页面

- 登录页: `frontend/src/pages/Login.vue`
- 注册页: `frontend/src/pages/Register.vue`
- 个人资料: `frontend/src/pages/Profile.vue`
- 设置页: `frontend/src/pages/Settings.vue`

## 覆盖接口（经网关）

- `POST /api/user/register`
- `POST /api/user/login`
- `PUT /api/user/profile`
- `GET /api/user/search`
- `POST /api/user/online`
- `POST /api/user/offline`
- `POST /api/user/heartbeat`
- `POST /api/user/online-status`

## 自动化验收

- 执行: `python test_im_complete.py --mode gateway --service user,auth`
- 产物: `test_reports/run_*/user/report.json`、`test_reports/run_*/auth/report.json`

## 正向流程（至少 3 条）

1. 注册 → 登录 → 进入聊天页
2. 登录后更新个人资料（昵称/邮箱）→ 刷新页面后信息一致
3. 搜索用户 → 发起好友请求（跳转到“好友与联系人”模块继续）

## 异常流程（至少 5 条）

1. 注册重复用户名（4xx）
2. 登录密码错误（4xx）
3. 登录态过期/Token 无效（401）
4. 3G 网络下登录接口超时（超时）
5. 断网后重复点击登录（网络断开）

## 性能与稳定性

- Chrome DevTools 禁用缓存 + 模拟 3G:
  - 首屏加载: <2s（记录值: ${first_paint_ms}ms）
  - 交互响应: <300ms（记录值: ${interaction_ms}ms）
- 控制台报错: 无/有（截图: ${console_screenshot_path}）

## 证据留存

- Network 报文/截图（含 `X-Gateway-Route`、`X-Trace-Id`）: ${network_artifacts_path}
- TraceId 对照（前端埋点/网关日志/应用日志/DB）: ${trace_matrix_path}
- 录屏: ${video_path}

## 结论

- 结论: 通过/不通过
- 阻塞项:
  - ${blocker_1}
  - ${blocker_2}

