# 模块联调报告：群组

## 基线信息

- 网关地址: ${gateway_host}:${gateway_port}
- 验证时间: ${date}
- 版本号: ${version}
- 验证人: ${owner}

## 覆盖页面

- 群组页: `frontend/src/pages/Groups.vue`
- 群组列表: `frontend/src/components/GroupList.vue`

## 覆盖接口（经网关）

- `POST /api/group/create`
- `GET /api/group/user/{userId}`
- `POST /api/group/members/list`
- `POST /api/group/{groupId}/join`
- `POST /api/group/{groupId}/leave`
- `PUT /api/group/{groupId}`
- `DELETE /api/group/{groupId}`

## 自动化验收

- 执行: `python test_im_complete.py --mode gateway --service group`
- 产物: `test_reports/run_*/group/report.json`

## 正向流程（至少 3 条）

1. 创建群组 → 自动加入 → 群列表可见
2. 拉取群成员列表 → 显示成员信息
3. 退出群组 → 群列表刷新

## 异常流程（至少 5 条）

1. 未登录调用群列表接口（401）
2. 创建群参数缺失/不合法（4xx）
3. 非成员拉取成员列表（4xx/403）
4. 3G 网络下加载群列表超时（超时）
5. 网关限流触发后的重试与提示（429/自定义限流码）

## 性能与稳定性

- Chrome DevTools 禁用缓存 + 模拟 3G:
  - 首屏加载: <2s（记录值: ${first_paint_ms}ms）
  - 交互响应: <300ms（记录值: ${interaction_ms}ms）
- 控制台报错: 无/有（截图: ${console_screenshot_path}）

## 证据留存

- Network 报文/截图（含 `X-Gateway-Route`、`X-Trace-Id`）: ${network_artifacts_path}
- TraceId 对照（前端埋点/网关日志/应用日志/DB）: ${trace_matrix_path}
- 录屏: ${video_path}

## 结论

- 结论: 通过/不通过
- 阻塞项:
  - ${blocker_1}
  - ${blocker_2}

