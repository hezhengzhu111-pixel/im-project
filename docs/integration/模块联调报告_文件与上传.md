# 模块联调报告：文件与上传

## 基线信息

- 网关地址: ${gateway_host}:${gateway_port}
- 验证时间: ${date}
- 版本号: ${version}
- 验证人: ${owner}

## 覆盖页面

- 聊天页上传: `frontend/src/pages/Chat.vue`
- 上传 Hook: `frontend/src/hooks/useFileUpload.ts`

## 覆盖接口（经网关）

- `POST /api/file/upload/file`
- `POST /api/file/upload/image`
- `POST /api/file/upload/audio`
- `POST /api/file/upload/video`
- `POST /api/file/upload/avatar`
- `POST /api/file/info`
- `POST /api/file/download`

## 自动化验收

- 执行: `python test_im_complete.py --mode gateway --service file`
- 产物: `test_reports/run_*/file/report.json`

## 正向流程（至少 3 条）

1. 上传普通文件 → 发送文件消息 → 可下载
2. 上传图片 → 发送图片消息 → 可预览
3. 上传头像 → 个人资料页展示更新

## 异常流程（至少 5 条）

1. 上传空文件/超限（4xx）
2. 非法文件类型（4xx）
3. 未登录上传（401）
4. 3G 网络下上传超时（超时）
5. 断网重连后续传/重试策略生效（网络断开）

## 性能与稳定性

- Chrome DevTools 禁用缓存 + 模拟 3G:
  - 首屏加载: <2s（记录值: ${first_paint_ms}ms）
  - 交互响应: <300ms（记录值: ${interaction_ms}ms）
- 控制台报错: 无/有（截图: ${console_screenshot_path}）

## 证据留存

- Network 报文/截图（含 `X-Gateway-Route`、`X-Trace-Id`）: ${network_artifacts_path}
- TraceId 对照（前端埋点/网关日志/应用日志/DB）: ${trace_matrix_path}
- 录屏: ${video_path}

## 结论

- 结论: 通过/不通过
- 阻塞项:
  - ${blocker_1}
  - ${blocker_2}

