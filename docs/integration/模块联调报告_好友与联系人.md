# 模块联调报告：好友与联系人

## 基线信息

- 网关地址: ${gateway_host}:${gateway_port}
- 验证时间: ${date}
- 版本号: ${version}
- 验证人: ${owner}

## 覆盖页面

- 好友页: `frontend/src/pages/Friends.vue`
- 联系人列表: `frontend/src/components/ContactsList.vue`

## 覆盖接口（经网关）

- `POST /api/friend/request`
- `POST /api/friend/accept`
- `POST /api/friend/reject`
- `GET /api/friend/list`
- `GET /api/friend/requests`
- `DELETE /api/friend/remove`
- `PUT /api/friend/remark`

## 自动化验收

- 执行: `python test_im_complete.py --mode gateway --service user`
- 产物: `test_reports/run_*/user/report.json`

## 正向流程（至少 3 条）

1. 搜索用户 → 发起好友请求 → 对端看到请求列表
2. 对端接受好友请求 → 双方好友列表更新
3. 修改好友备注 → 好友列表展示更新

## 异常流程（至少 5 条）

1. 对自己发起好友请求（4xx）
2. 目标用户不存在（4xx）
3. 未登录调用好友列表（401）
4. 3G 网络下请求列表加载超时（超时）
5. 网关限流触发后提示与重试（429/自定义限流码）

## 性能与稳定性

- Chrome DevTools 禁用缓存 + 模拟 3G:
  - 首屏加载: <2s（记录值: ${first_paint_ms}ms）
  - 交互响应: <300ms（记录值: ${interaction_ms}ms）
- 控制台报错: 无/有（截图: ${console_screenshot_path}）

## 证据留存

- Network 报文/截图（含 `X-Gateway-Route`、`X-Trace-Id`）: ${network_artifacts_path}
- TraceId 对照（前端埋点/网关日志/应用日志/DB）: ${trace_matrix_path}
- 录屏: ${video_path}

## 结论

- 结论: 通过/不通过
- 阻塞项:
  - ${blocker_1}
  - ${blocker_2}

