# 网关转发验证报告

## 基线信息

- 网关地址: ${gateway_host}:${gateway_port}
- 验证时间: ${date}
- 版本号: ${version}
- 验证人: ${owner}

## 强制约束核对

- 所有 HTTP 请求经网关转发: 通过/不通过（证据见下方）
- 请求头包含 `X-Gateway-Route: true`: 通过/不通过
- 链路追踪 `X-Trace-Id` 贯穿始终（请求→网关→后端）: 通过/不通过

## 证据留存

- Chrome Network 截图（示例接口）:
  - 文件路径: ${network_screenshot_path}
  - 说明: ${network_screenshot_note}
- 网关日志（包含 traceId、method、path）:
  - 文件路径: ${gateway_log_path}
  - 说明: ${gateway_log_note}

## 自动化校验

### 1) 网关头校验用例

- 执行: `python test_im_complete.py --mode gateway --service gateway`
- 预期:
  - 未携带 `X-Gateway-Route` 的非白名单路径返回 400
  - `X-Trace-Id` 在响应头回写且与请求一致
- 产物: `test_reports/run_*/gateway/report.json`

### 2) 200 并发压测

- 执行: `python scripts/gateway_load_test.py --method GET --count 200 --concurrency 200`
- 阈值:
  - 网关 5xx 错误率 < 0.1%
  - p99 延迟 < 500ms
- 结果文件: `test_reports/gateway_load_*.json`

## 压测结论

- 结论: 通过/不通过
- 不通过原因与阻塞项:
  - ${blocker_1}
  - ${blocker_2}

