import 'profile_page.dart';

/// Alias for [ProfilePage] that preserves the existing public API used by
/// desktop and mobile routes.
class ProfileSettingsPage extends ProfilePage {
  const ProfileSettingsPage({super.key});
}
