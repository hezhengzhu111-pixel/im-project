server:
  port: 8084

spring:
  application:
    name: im-auth-service
  config:
    import:
      - optional:classpath:dev/application-mysql.yml
      - optional:classpath:dev/application-redis.yml
      - optional:classpath:dev/application-kafka.yml
      - optional:classpath:dev/application-nacos.yml
  profiles:
    active: default
  threads:
    virtual:
      enabled: false

jwt:
  secret: ${JWT_SECRET:im-access-secret-im-access-secret-im-access-secret-im-access-secret}
  expiration: 86400000
  header: Authorization
  prefix: "Bearer "

auth:
  refresh:
    secret: ${AUTH_REFRESH_SECRET:im-refresh-secret-im-refresh-secret-im-refresh-secret-im-refresh-secret}
    expiration: 604800000
  resource-cache:
    ttl-seconds: 3600
  ws-ticket:
    ttl-seconds: 30
  revoke:
    token-ttl-seconds: 86400

im:
  auth:
    cookie:
      same-site: ${IM_AUTH_COOKIE_SAME_SITE:Lax}
      secure: ${IM_AUTH_COOKIE_SECURE:auto}
      ws-ticket-name: ${IM_WS_TICKET_COOKIE_NAME:IM_WS_TICKET}
      ws-ticket-same-site: ${IM_WS_TICKET_COOKIE_SAME_SITE:Lax}
      ws-ticket-secure: ${IM_WS_TICKET_COOKIE_SECURE:auto}
      ws-ticket-path: ${IM_WS_TICKET_COOKIE_PATH:/websocket}
  virtual-threads:
    enabled: false
  logging:
    root-dir: ../logs
  snowflake:
    worker-id: 2
    datacenter-id: 1
  internal:
    header: X-Internal-Secret
    secret: ${IM_INTERNAL_SECRET:im-internal-secret-im-internal-secret-im-internal-secret-im}
  gateway:
    user-id-header: X-User-Id
    username-header: X-Username
    auth:
      secret: ${IM_GATEWAY_AUTH_SECRET:im-gateway-auth-secret-im-gateway-auth-secret-im-gateway-auth-secret}
      max-skew-ms: 300000
  security:
    token-revocation-check:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    root: INFO
    com.im: INFO
